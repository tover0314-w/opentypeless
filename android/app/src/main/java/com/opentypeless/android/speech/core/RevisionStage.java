package com.opentypeless.android.speech.core;

/** Stability owned by Speech Core, independent from a provider's final callback. */
public enum RevisionStage {
    LIVE,
    PROVISIONAL,
    REFINED,
    USER_LOCKED
}
