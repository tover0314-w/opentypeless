package com.opentypeless.android.editor;

/** Allowlisted semantic editor actions; the Android host owns the platform-ID mapping. */
public enum EditorAction {
    GO,
    SEARCH,
    SEND,
    NEXT,
    DONE,
    PREVIOUS
}
